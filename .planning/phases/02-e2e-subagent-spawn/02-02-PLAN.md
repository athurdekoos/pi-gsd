---
phase: 02-e2e-subagent-spawn
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/e2e-subagent.test.ts
  - tests/run-all.ts
autonomous: true
requirements:
  - E2E-03
  - E2E-04

must_haves:
  truths:
    - "SUMMARY.md artifact exists at .planning/research/SUMMARY.md after agent completes"
    - "SUMMARY.md is non-empty and contains recognizable markers from input files"
    - "Test suite is registered in run-all.ts under E2E category"
  artifacts:
    - path: "tests/e2e-subagent.test.ts"
      provides: "Complete e2e subagent test with all 4 requirement tests"
      contains: "E2E-03"
    - path: "tests/run-all.ts"
      provides: "Updated test runner with e2e-subagent registration"
      contains: "e2e-subagent.test.ts"
  key_links:
    - from: "tests/run-all.ts"
      to: "tests/e2e-subagent.test.ts"
      via: "SUITES array registration under E2E category"
      pattern: "e2e-subagent\\.test\\.ts.*E2E"
---

<objective>
Add E2E-03 (artifact existence) and E2E-04 (content verification with sentinel checks) tests to the e2e-subagent test file, then register it in run-all.ts.

Purpose: Complete the subagent pipeline verification — prove the agent not only spawns and reads files, but actually produces the expected output artifact with synthesized content.
Output: Complete test file with all 4 E2E tests + registration in the unified runner.
</objective>

<execution_context>
@/home/mia/dev/pi-gsd/gsd/workflows/execute-plan.md
@/home/mia/dev/pi-gsd/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-e2e-subagent-spawn/02-CONTEXT.md
@.planning/phases/02-e2e-subagent-spawn/02-RESEARCH.md
@.planning/phases/02-e2e-subagent-spawn/02-01-SUMMARY.md

<interfaces>
<!-- run-all.ts registration pattern -->
From tests/run-all.ts (SUITES array, lines 32-58):
```typescript
interface SuiteDef {
  file: string;
  category: string;
}

const SUITES: SuiteDef[] = [
  // ...existing entries...
  // E2E (conditional)
  { file: "e2e-smoke.test.ts", category: "E2E" },
  // ...more entries...
];
```

E2E gating logic (lines 161-176):
- `const e2eEnabled = process.argv.includes("--e2e");`
- Suites with `category: "E2E"` are skipped when `--e2e` not set
- E2E suites receive `["--e2e"]` as args when enabled

<!-- Sentinel and workspace pattern from Plan 02-01 -->
The test file from Plan 02-01 has:
- `SENTINELS` object with STACK, FEATURES, ARCHITECTURE, PITFALLS keys
- `ws: TempWorkspace` with `.planning/research/` directory seeded
- `session: PiRpcSession` that has already completed the subagent turn
- Tests run inside `runTests()` async function with shared session
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E2E-03 and E2E-04 tests to e2e-subagent.test.ts</name>
  <files>tests/e2e-subagent.test.ts</files>
  <action>
Add two more test functions inside the `runTests()` function in `tests/e2e-subagent.test.ts`, after the existing E2E-01 and E2E-02 tests. These tests use the same shared `ws` (TempWorkspace) and `session` (PiRpcSession) that were set up in Plan 02-01.

**E2E-03 test:** "E2E-03: spawned agent writes SUMMARY.md artifact to .planning/research/SUMMARY.md"
- Assert: `ws.exists(".planning/research/SUMMARY.md")` is true
- Use `formatFailure()`:
  - file: `agents/gsd-research-synthesizer.md`
  - expected: `SUMMARY.md exists at .planning/research/SUMMARY.md after agent completes`
  - actual: `File not found — agent may not have written output`
  - why: `E2E-03 requires the synthesizer agent to write its artifact to the expected path`

**E2E-04 test:** "E2E-04: SUMMARY.md contains recognizable content from input files"
- Read SUMMARY.md content: `const summary = ws.readFile(".planning/research/SUMMARY.md");`
- **Length check:** Assert `summary.length >= 200` (non-trivial content)
  - formatFailure: expected ≥200 chars, actual length, why: agent should produce substantial synthesis
- **Heading check:** Assert summary contains at least one markdown heading (`summary.includes("#")`)
  - formatFailure: expected markdown structure, actual: no headings found
- **Sentinel check:** Count how many of the 4 sentinel strings appear in the summary:
  ```typescript
  const sentinelHits = Object.entries(SENTINELS)
    .filter(([_, sentinel]) => summary.includes(sentinel));
  ```
  Assert `sentinelHits.length >= 2` (at least 2 of 4 sentinels preserved)
  - formatFailure:
    - file: `tests/e2e-subagent.test.ts`
    - expected: `≥2 of 4 sentinel strings present in SUMMARY.md`
    - actual: `Found ${sentinelHits.length}/4: ${sentinelHits.map(([k]) => k).join(", ") || "none"}`
    - why: `E2E-04 verifies the agent actually read and synthesized input content, not hallucinated`
    - evidence: First 300 chars of summary content for debugging

**IMPORTANT:** If E2E-03 fails (file doesn't exist), E2E-04 should handle the error gracefully — wrap the readFile in a try/catch and fail with a clear message rather than crashing the suite.
  </action>
  <verify>
    <automated>grep -c "E2E-0[34]" tests/e2e-subagent.test.ts</automated>
  </verify>
  <done>
    - E2E-03 test checks SUMMARY.md existence in workspace
    - E2E-04 test checks length ≥200, has markdown heading, ≥2/4 sentinels present
    - Both tests use formatFailure for diagnostic messages
    - E2E-04 handles missing file gracefully (doesn't crash if E2E-03 failed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Register e2e-subagent.test.ts in run-all.ts</name>
  <files>tests/run-all.ts</files>
  <action>
Add the new test file to the `SUITES` array in `tests/run-all.ts`.

**Location:** Insert after the existing `e2e-smoke.test.ts` entry (line ~56), within the E2E category comment block.

**Add this entry:**
```typescript
  { file: "e2e-subagent.test.ts", category: "E2E" },
```

**Also update the module header comment** (line 1-2) to reflect the new suite count. Currently says "16 test suites" — update to the correct count after adding the new entry.

This gets automatic `--e2e` gating from the existing run-all.ts logic — no other changes needed.
  </action>
  <verify>
    <automated>grep "e2e-subagent" tests/run-all.ts</automated>
  </verify>
  <done>
    - `e2e-subagent.test.ts` appears in SUITES array with category "E2E"
    - Suite count in header comment is accurate
    - Running `npx tsx tests/run-all.ts` (without --e2e) shows "Skipping e2e-subagent.test.ts"
  </done>
</task>

</tasks>

<verification>
- All 4 E2E tests exist in `tests/e2e-subagent.test.ts` (E2E-01 through E2E-04)
- Running `npx tsx tests/e2e-subagent.test.ts` (no --e2e) exits 0 with skip message
- `tests/run-all.ts` has `e2e-subagent.test.ts` in SUITES under E2E category
- Running `npx tsx tests/run-all.ts` (no --e2e) shows skip for both e2e-smoke and e2e-subagent
- When `--e2e` is provided, e2e-subagent.test.ts runs all 4 tests against a real Pi process
</verification>

<success_criteria>
- E2E-03 test verifies SUMMARY.md artifact exists at expected path
- E2E-04 test verifies content: length ≥200 chars, has heading, ≥2/4 sentinels present
- All assertions use formatFailure for diagnostic output
- Test file registered in run-all.ts and receives --e2e gating automatically
- Full e2e run (with --e2e flag) exercises the complete subagent pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/02-e2e-subagent-spawn/02-02-SUMMARY.md`
</output>
